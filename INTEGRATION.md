# みらいコンパス × みらいパスポート 統合設計

このドキュメントは、2つのアプリ（みらいコンパス＝自由進度学習の計画・見取り／
みらいパスポート＝AIワークシートの作成・提出・添削）を **単一プロジェクトへ統合する**
ための設計と移行手順をまとめたものです。

## ✅ 現在のステータス：単一プロジェクトへの統合は実装済み

**ワークシートの作成・回答・添削は、みらいコンパスのこのプロジェクトに取り込まれました。**
別アプリだった「みらいパスポート」は、この統合プロジェクトに**置き換えられました（役割を終えています）**。
以下がこの1プロジェクトの中で完結しています。

- **認証**: サーバーが `Session.getActiveUser().getEmail()` で呼び出し元を判定。先生はメール許可リスト
  （`TEACHER_EMAILS`）。初期設定を実行した人が最初の先生、以後は「先生の管理」で追加。パスワードは廃止。
- **DB の統合**: ワークシート用の `Worksheets` / `Responses` シートをコンパスの DB に同居。
- **連携の内部化**: 旧 `ImportQueue` 直書き・`doPost` Webhook・起動用 URL 受け渡しを撤去し、
  先生の `createWorksheetsForUnit`（内部関数）に置き換え。
- **ワークシート UI の同梱**: `js_worksheet.html`（自己完結オーバーレイ）をアプリ内に追加。
- **fabric.js の自己ホスト**: 手書き／赤ペン添削キャンバス用に vendor へ同梱（pdf.js/OCR・pako は非同梱）。

> かつて「両リポジトリにバイト単位で同一のコピーを置く」としていた制約は**もう不要**です
> （Passport は superseded）。本ファイルはコンパス単一プロジェクトの実情に合わせて自由に更新して構いません。
> 積み残し・未検証項目は本ファイル末尾と `AUDIT.md` を参照してください。

---

## 1. なぜ統合するのか

2つは同じ「1クラス・40人・GIGA端末」を対象にした姉妹アプリで、対象児童・単元・
教員が完全に重なります。かつて両者は別 GAS プロジェクト・別スプレッドシートで動き、
次の3経路だけで細く繋がっていました。**統合により、この3経路はすべて撤去され、
アプリ内の関数呼び出しに置き換わりました。**

| 経路（旧） | かつての実装 | 統合後 |
|---|---|---|
| ① 状態同期（SOS/集中） | Passport `syncToCompass` → Compass `doPost` | **撤去**。同一プロジェクト内で `LiveStatus` を直接更新 |
| ② 単元計画の受け渡し | Compass が Passport の `ImportQueue` シートへ**直接書き込み** | **撤去**。先生の `createWorksheetsForUnit`（内部関数）に置換 |
| ③ 児童の起動・同定 | Compass が `?studentId=&studentName=` で Passport を起動 | **撤去**。児童はアプリ内でタスクのワークシートを開く（別アプリ・別ログイン無し） |

これで DB は1つ、認証は `getActiveUser` の1系統になりました。

---

## 2. すでに敷いた共通契約（本改修で導入）

統合の前提となる「両アプリで同一のもの」を先に入れました。**バイト単位で同一**に保ってください。

### 2.1 `MiraiShared`（クライアント / `js_core.html`）
- 安全描画ヘルパー: `escHtml` / `escUrl`（`javascript:` 等を遮断、`data:image/` は許可）/ `escJs`
- 共通enum（統合後の唯一の語彙）と正規化関数:
  - **在席モード** `normal | focus | sos` … `normPresence(v)`
  - **学習進捗** `todo | doing | done` … `normProgress(v)`
  - **提出状態** `draft | submitted | graded` … `normSubmission(v)`

### 2.2 `MiraiAuth`（サーバー / `code.gs`）
- `CacheService` に置く短命トークン（6時間）による**先生の本人確認**。
- `issueToken()` / `isValid(token)` / `requireTeacher(token)` / `revoke(token)`。
- **デプロイ設定（実行者・アクセス範囲）に依存しない**ため、本番で認可が原因で授業が
  止まりません。ここが従来「本番で確かめられないから触れない」とされていた B5 の回避策です。

> `MiraiShared` は据え置き。`MiraiAuth` は下記の getActiveUser 方式へ格上げ済みです
> （旧トークン方式の記述は歴史的経緯です）。

---

## 3. 認可モデル（現状＝実装済み）

| 呼び出し | 認可 |
|---|---|
| 児童向け関数 | サーバーが `Session.getActiveUser().getEmail()` で本人を判定。回答（`Responses`）はメールで本人スコープ化 |
| 先生専用関数 | **サーバー本文の先頭で `MiraiAuth.requireTeacher()`**。呼び出し元メールを `TEACHER_EMAILS` と照合。クライアントは ID を渡さない（`runTeacher(...)` はトークンを前置しない） |
| 先生の任命 | 初期設定を実行した人が最初の先生。以後は「先生の管理」でメールを追加 |
| 初期化 | 初回（DB未作成）のみ無認証、既存システムの再実行は先生のみ |
| パスワード | **廃止**（`TEACHER_PASS` / `verifyPassword` は削除。品質ゲート `SEC_NO_PASSWORD_AUTH` が復活を防ぐ） |

**成立条件**: デプロイを **「実行: 自分（所有者）／アクセス: 同一 Workspace（Googleアカウントを持つ全員）」**
にすること。消費者向け（非 Workspace）アカウントでは `getActiveUser` が `""` を返し、先生判定が働きません。

**残る限界**: コンパス由来の名前キーのテーブル（`StudentPlans` / `Portfolios` 等）は、いまも児童の
表示名をキーにしており、同名衝突・分離の弱さが残ります（ワークシートの `Responses` はメールで
本人スコープ化済み）。→ 6章の未決事項。

---

## 4. 連携トークンの設定手順（廃止済み）

旧・別アプリ時代に必要だった「パスポート連携キー（`INTEGRATION_TOKEN`）」の設定手順は、
`doPost` Webhook・`ImportQueue` 直書きの撤去に伴い**不要になりました**。同一プロジェクト内の
関数呼び出しになったため、SOS・集中・状態は追加設定なしで LIVE 画面へ反映されます。

---

## 5. 統合ロードマップ（達成状況）

### フェーズ0 ✅ 完了 — 共通契約を敷く
- `MiraiShared` / `MiraiAuth` を導入。保存型XSS・データ破損・無認可の主要な穴を塞ぐ。

### フェーズ1 🟡 一部 — 児童IDと課題IDの統一
- **課題ID**: 単元スコープの一意 ID に統一済み。ワークシートはタスクにひも付く。
- **児童ID（積み残し）**: ワークシートの `Responses` はメールで本人スコープ化済み。ただし
  コンパス由来の `StudentPlans` / `Portfolios` 等は**まだ表示名キー**で、`studentId` への
  付け替え（と先生集計側の追随）は未了。→ 6章。

### フェーズ2 ✅ 完了 — 連携をやめ、内部関数へ
- ② `ImportQueue` 直書きを**撤去**し、先生の `createWorksheetsForUnit`（内部関数）に置換。
- ③ 児童同定は `getActiveUser` に一本化（別アプリ起動 URL・署名トークンの受け渡しは不要に）。
- 状態・提出語彙は共通 enum に統一。

### フェーズ3 🟡 一部 — DBとシェルの統合
- **DB**: `Worksheets` / `Responses` をコンパスの DB に同居（完了）。
- **シェル（未検証）**: `docs/` PWAシェルは1枚。`manifest` の `id/scope/start_url` と `sw.js` の
  `CACHE_PREFIX` はコンパス用に確定済みだが、`gigayama.github.io` での実 PWA 動作は未検証。
- `appsscript.json` / OAuth スコープのリポジトリ収録・監査は未着手。

### フェーズ4 ✅ 一部 — 品質ゲート
- `SEC_PRIVILEGED_FN_GUARDED`（先生専用関数の未ガード検出）と `SEC_NO_PASSWORD_AUTH`
  （パスワード認証の回帰防止）を追加。フリート共通正本 `project-quality.mjs` の確定は将来課題。

---

## 6. まだ開いている論点（人間の判断が要る）

決着した論点は上部ステータスのとおり（教員認証＝`getActiveUser` 方式に格上げ／AI＝2経路併存／
デプロイ＝1プロジェクトに完全統合）。以下はまだ残っています。

| 論点 | 選択肢・現状 |
|---|---|
| 名前キーの ID 化 | `StudentPlans` / `Portfolios` 等を `studentId` に付け替えるか（同名衝突・分離の弱さが残っている） |
| ポートフォリオの正本 | コンパス `Portfolios`＋`DailyReflections` と、ワークシート `Responses` の振り返りをどう一本化するか |
| 児童の相互閲覧 | 「ひろば／ギャラリー」ピア閲覧、`mode=dashboard` 管理画面、下部4タブナビの整理（いずれも未実装） |

---

© 2026 みらいコンパス Project（旧・みらいパスポートを統合）
