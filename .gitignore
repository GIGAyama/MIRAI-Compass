node_modules/
dist/
.clasp.json
.clasprc.json
.env
.env.*
.DS_Store
Thumbs.db
*.log
npm-debug.log*

# 検査・実測の作業ディレクトリ（成果物ではない）
.audit/

# clasp（GASのプッシュ設定。スクリプトIDが入るので置かない）

# 反映前のGASの控え（CIの成果物として持ち出すので、リポジトリには置かない）
dist/gas-before-push/
