# 🚀 みらいコンパス デプロイ & スモークテスト手順

統合後のみらいコンパス（ワークシート機能を内蔵）を、実際に Google Apps Script に
デプロイして使えるようにするための、具体的なチェックリストです。

> **前提（重要）**: 本アプリは本人確認に `Session.getActiveUser().getEmail()` を使います。
> **学校の Google Workspace アカウント**で使うことが前提です。消費者向け（個人 Gmail など、
> Workspace でない）アカウントでは `getActiveUser` が空文字 `""` を返し、先生判定が働きません。
> したがって**下記のデプロイ設定（実行: 自分／アクセス: 同一 Workspace）が必須**です。

---

## 1. 生成物（vendor файлы）を作る

手元（Node.js 20 以上）で:

```bash
npm ci
npm run build      # vendor_css.html / vendor_icons.html / vendor_js.html を生成
```

- `vendor_*.html` はライブラリ本体（Bootstrap / SweetAlert2 / Chart.js / Sortable / fabric.js /
  Bootstrap Icons）で、**GAS に貼るファイルに含まれます**。
- 併せて `npm run ci` が緑になることを確認しておくと安心です
  （build → verify-generated → check → --self-test → test）。

---

## 2. GAS プロジェクトに 10 個のファイルを作って貼る

[Google Apps Script](https://script.google.com/) で「新しいプロジェクト」を作成し、
以下の **10 個**のファイルを作って、それぞれ中身を貼り付けます。

| ファイル | 種類 | 備考 |
|---|---|---|
| `code.gs` | スクリプト | サーバー本体 |
| `index.html` | HTML | エントリーポイント |
| `css.html` | HTML | スタイル |
| `js_core.html` | HTML | 共通・初期設定・設定画面 |
| `js_student.html` | HTML | 児童画面 |
| `js_teacher.html` | HTML | 先生画面 |
| `js_worksheet.html` | HTML | ワークシート（作成・回答・添削） |
| `vendor_css.html` | HTML | ⚠️ 生成物（手で編集しない） |
| `vendor_icons.html` | HTML | ⚠️ 生成物（手で編集しない） |
| `vendor_js.html` | HTML | ⚠️ 生成物（手で編集しない） |

- `code.gs` 以外は、拡張子 `.html` で作成してください。
- `vendor_*.html` は生成物です。更新は手元で `npm run build` し直して貼り替えます。

---

## 3. ウェブアプリとしてデプロイする

「デプロイ」→「新しいデプロイ」→ 種類「**ウェブアプリ**」:

- **次のユーザーとして実行**: **「自分（所有者）」**
- **アクセスできるユーザー**: **「同一 Workspace（Googleアカウントを持つ全員）」**

この組み合わせでないと `getActiveUser` が実メールを返さず、先生・児童の判定が働きません。
発行された **ウェブアプリ URL（`.../exec`）** を控えます。

---

## 4. 初回セットアップ

1. ウェブアプリ URL を、**自分（先生）の Workspace アカウント**で開く。
2. 「**初期設定を開始する**」を押す（DB 用スプレッドシートが自動作成される）。
   → これを押した**あなたが「最初の先生」**になります（パスワードはありません）。
3. 先生モードの **設定（⚙）→「先生の管理（メールアドレス）」** で、同僚の先生の
   メールアドレスを追加する。
4. （任意）**設定 →「AI設定（Geminiキー）」** で Gemini API キーを登録する。
   未登録でも、ワークシートは手貼り HTML で作れます。

---

## 5. スモークテスト（実機で必ず1周する）

### ワークシートの1周（作成 → 配布 → 回答 → 添削 → 返却）

- [ ] 先生: 「**設計**」で単元を作る（新規単元＋タスクを数個）。
- [ ] 先生: 「**ワークシート作成**」を押し、その単元のワークシートを用意する
      （タスク編集の「ワークシート作成・編集」でも可。AI か手貼りで中身を入れる）。
- [ ] 児童（別アカウント）: 「今日の学習」でそのタスクを開くと、**その場でワークシートが開く**。
- [ ] 児童: **開いた直後に答えの欄へ文字が打てる**（入力優先）。
- [ ] 児童: 「**手書き**」ボタンを押すとペンで書ける。もう一度押すと文字入力に戻る。
- [ ] 児童: 「**提出**」を押す。
- [ ] 先生: 提出物を開き、**赤ペンで添削**して「**返却**」を押す。
- [ ] 児童: 「**先生コメント**」が届き、添削が見える。

### LIVE・在席

- [ ] 児童が「**SOS**」を押すと、先生の **LIVE** 画面でその子のカードが赤く光る。
- [ ] 「**集中**」など在席モードの変化が LIVE に反映される。

### PWA（`docs/` シェル）

- [ ] `https://<ユーザー名>.github.io/MIRAI-Compass/` を開き、GAS の `/exec` URL を設定。
- [ ] Chrome の「インストール」から**アプリとしてインストール**でき、独立ウィンドウで起動する。
- [ ] オフライン時に `offline.html` が出る。

---

## 6. つまずきやすい点

- **「先生として登録されていません」**: 開いているアカウントが Workspace 外か、まだ許可リストに
  無い。既存の先生に「先生の管理」で追加してもらう。
- **先生判定がまったく働かない**: デプロイのアクセス範囲が「全員（匿名を含む）」になっていないか
  確認（`getActiveUser` が `""` になる）。「同一 Workspace」にする。
- **iframe 内でログインできない**: シェルの設定から「新しいタブで開く」で一度ログインする。

© 2026 みらいコンパス Project（旧・みらいパスポートを統合）
